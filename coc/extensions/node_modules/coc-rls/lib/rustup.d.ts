/// <reference types="node" />
import * as child_process from 'child_process';
export declare class RustupConfig {
    readonly channel: string;
    readonly path: string;
    constructor(channel: string, path: string);
}
export declare function runRlsViaRustup(env: any, config: RustupConfig): Promise<child_process.ChildProcess>;
export declare function rustupUpdate(config: RustupConfig): Promise<void>;
/**
 * Parses given output of `rustup show` and retrieves the local active toolchain.
 */
export declare function parseActiveToolchain(rustupOutput: string): string;
/**
 * Returns active (including local overrides) toolchain, as specified by rustup.
 * May throw if rustup at specified path can't be executed.
 */
export declare function getActiveChannel(rustupPath: string, wsPath: string): string;
