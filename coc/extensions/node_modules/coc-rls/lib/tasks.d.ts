import { TerminalResult } from 'coc.nvim';
export interface Cmd {
    binary: string;
    args: string[];
    env: {
        [key: string]: string;
    };
}
export declare function runCommand(cwd: string, command: Cmd): Promise<TerminalResult>;
