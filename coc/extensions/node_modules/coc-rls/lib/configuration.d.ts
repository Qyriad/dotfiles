import { RevealOutputChannelOn } from 'coc.nvim';
import { RustupConfig } from './rustup';
export declare class RLSConfiguration {
    readonly rustupPath: string;
    readonly logToFile: boolean;
    readonly rustupDisabled: boolean;
    readonly revealOutputChannelOn: RevealOutputChannelOn;
    readonly updateOnStartup: boolean;
    readonly channel: string;
    /**
     * If specified, RLS will be spawned by executing a file at the given path.
     */
    readonly rlsPath: string | null;
    private readonly configuration;
    private readonly wsPath;
    static loadFromWorkspace(wsPath: string): RLSConfiguration;
    private constructor();
    rustupConfig(): RustupConfig;
    private static readRevealOutputChannelOn;
    /**
     * Tries to fetch the `rust-client.channel` configuration value. If missing,
     * falls back on active toolchain specified by rustup (at `rustupPath`),
     * finally defaulting to `nightly` if all fails.
     */
    private static readChannel;
}
