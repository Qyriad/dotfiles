import { ExtensionContext } from 'coc.nvim';
interface API {
    configurePlugin(pluginId: string, configuration: {}): void;
}
export declare function activate(context: ExtensionContext): Promise<API>;
export {};
