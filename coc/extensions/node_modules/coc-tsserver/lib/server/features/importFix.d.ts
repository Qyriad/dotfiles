import { CodeActionProvider } from 'coc.nvim';
import BufferSyncSupport from './bufferSyncSupport';
import { TextDocument, Range, CodeActionContext, CancellationToken, CodeAction } from 'vscode-languageserver-protocol';
export default class ImportFixProvider implements CodeActionProvider {
    private readonly bufferSyncSupport;
    constructor(bufferSyncSupport: BufferSyncSupport);
    provideCodeActions(document: TextDocument, _range: Range, context: CodeActionContext, _token: CancellationToken): Promise<CodeAction[]>;
}
