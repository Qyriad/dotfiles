export declare function runCommand(cmd: string, cwd: string, timeout?: number): Promise<string>;
export declare function moduleExists(name: string): Promise<boolean>;
/**
 * Removes duplicates from the given array. The optional keyFn allows to specify
 * how elements are checked for equalness by returning a unique string for each.
 */
export declare function distinct<T>(array: T[], keyFn?: (t: T) => string): T[];
export declare function installModules(uri: string, names: string[]): Promise<void>;
